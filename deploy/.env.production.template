# Core
POSTGRES_PASSWORD=change_me_strong

# Runtime identity
APP_UID=10001
APP_GID=10001

# OCI secret mount
OCI_SECRETS_DIR=/etc/oci-cost-manager/oci
OCI_READ_GID=10010
OCI_CONFIG_PROFILE=DEFAULT

# OCI tenant settings are managed by app settings or secret resolver.
