POSTGRES_PASSWORD=change_me_strong
APP_UID=10001
APP_GID=10001
OCI_SECRETS_DIR=/etc/oci-cost-manager/oci
OCI_READ_GID=0
OCI_CONFIG_PROFILE=DEFAULT
# APP_MASTER_KEY is auto-generated on first run and persisted to the app_data
# volume. Override here only if you need to migrate from a known key.
# APP_MASTER_KEY=
ALLOW_OCI_FILE_PATH_MODE=true
